
{
  "name": "cah-payload-cms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'build skipped'",
    "start": "node dist/server.js",
    "dev": "ts-node server.ts"
  },
  "dependencies": {
    "payload": "2.0.0",
    "@payloadcms/bundler-webpack": "1.0.7",
    "@payloadcms/db-mongodb": "1.0.2",
    "@payloadcms/richtext-slate": "1.3.4",
    "express": "4.18.2",
    "dotenv": "16.3.1",
    "cross-env": "7.0.3"
  },
  "devDependencies": {
    "typescript": "5.2.2",
    "@types/express": "4.17.21",
    "@types/node": "20.0.0",
    "ts-node": "10.9.1"
  },
  "engines": {
    "node": "20.x"
  }
}
```

Also add `--legacy-peer-deps` to fix dependency conflicts. Go to Vercel → your Payload project → **Settings** → **Build & Output Settings** → change **Install Command** to:
```
npm install --legacy-peer-deps
